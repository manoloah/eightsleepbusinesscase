# Railway Deployment Configuration for Metabase
# This file contains the environment variables needed for Railway deployment

# Metabase Database Configuration
# For Railway, you can either:
# 1. Use Railway's PostgreSQL service (recommended)
# 2. Use your existing Supabase database

# Option 1: Railway PostgreSQL (uncomment and configure if using Railway's database)
# MB_DB_TYPE=postgres
# MB_DB_DBNAME=metabase
# MB_DB_PORT=5432
# MB_DB_USER=postgres
# MB_DB_PASS=${RAILWAY_POSTGRES_PASSWORD}
# MB_DB_HOST=${RAILWAY_POSTGRES_HOST}

# Option 2: Supabase Database (recommended for your use case)
MB_DB_TYPE=postgres
MB_DB_DBNAME=postgres
MB_DB_PORT=5432
MB_DB_USER=postgres
MB_DB_PASS=${SUPABASE_DB_PASSWORD}
MB_DB_HOST=db.lmokzxpktcchregvna.supabase.co

# Additional Metabase Configuration
JAVA_TIMEZONE=UTC

# SSL Configuration for Supabase
MB_DB_SSL=true
MB_DB_SSL_MODE=require

# Metabase Application Settings
MB_ENCRYPTION_SECRET_KEY=${MB_ENCRYPTION_SECRET_KEY:-your-secret-key-here}
MB_PASSWORD_COMPLEXITY=strong
MB_PASSWORD_LENGTH=8
